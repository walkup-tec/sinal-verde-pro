# Próximas tarefas

- Isolar um projeto Supabase de **staging** e apontar o `.env.local` do PC para ele (hoje o host local costuma usar o banco de produção).
- Quando houver demanda: evoluir o CRM em cima do `main` atual — não reabrir o plano Lovable de mock-only.
- Manter keepalive do Supabase Free ativo enquanto o plano não for pago.
